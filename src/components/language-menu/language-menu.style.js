import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pickerContainer: {
    width: 200,
    marginBottom: 20,
    borderWidth: 1,
    backgroundColor: 'white',
    alignSelf: 'center',
  },
});
